# Thomas Merkel <tm@core.io>#

##
## homebrew
##
# source the bash completion plugin
source /usr/local/etc/bash_completion

# set env PATH variable
PATH=/usr/local/bin:/usr/local/sbin:$(brew --prefix coreutils)/libexec/gnubin:$PATH:/opt/prince/bin

# grc
GRC=$(which grc)
if [ "$TERM" != dumb ] && [ -n "$GRC" ]; then
	alias colourify="$GRC -es --colour=auto"
	alias configure='colourify ./configure'
	alias diff='colourify diff'
	alias make='colourify make'
	alias gcc='colourify gcc'
	alias g++='colourify g++'
	alias as='colourify as'
	alias gas='colourify gas'
	alias ld='colourify ld'
	alias netstat='colourify netstat'
	alias ping='colourify ping'
	alias traceroute='colourify /usr/sbin/traceroute'
fi

##
## some osx aliases
##
alias ls='ls --color=auto'
alias grep='grep --color=auto'
alias fgrep='fgrep --color=auto'
alias egrep='egrep --color=auto'
alias airport='sudo /System/Library/PrivateFrameworks/Apple80211.framework/Versions/Current/Resources/airport'

##
## python virtual environment
##
WORKON_HOME="${TM_HOME}/.virtualenvs"
source /usr/local/bin/virtualenvwrapper.sh

##
## github settings
##
export GITHUB_USER='drscream'
export GITHUB_PASSWORD="$(security 2>&1 >/dev/null find-generic-password -gs 'github.com' | ruby -e 'print $1 if STDIN.gets =~ /^password: \"(.*)\"$/')"

##
## special commands
##
# dircolors exists only in brew repo, so run this command later
[[ -f ${HOME}/.dir_colors ]] && eval $(dircolors -b ${HOME}/.dir_colors)


# vim:ft=sh
